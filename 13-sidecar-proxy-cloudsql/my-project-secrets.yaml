# kubectl create secret generic my-project-secrets --from-literal=RDS_PASSWORD=<PASSWORD> \
# --from-literal=TICKET_DEVICE=<TICKET_D> --from-literal=TICKET_USER=<TICKET_U> \
# --from-literal=SMTP_USER=<EMAIL> --from-literal=SMTP_PASSWORD=<PASS_SMTP>

# echo -n dummytodos | base64
